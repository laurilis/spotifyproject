

function Playlist (props) {
    return (
        <div>
        <input></input>
        <button>Save to Spotify</button>
        <Track  title={track.title} 
                artist={track.artist} 
                album={track.album} />
        </div>
                                    
                                       
    )
}

export default Playlist;